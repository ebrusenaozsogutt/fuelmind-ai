<UserControl x:Class="FuelMind.Desktop.Views.PumpDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:FuelMind.Desktop.Controls"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28">
            <Button HorizontalAlignment="Left" Command="{Binding BackCommand}" Content="← Pompalara Dön" />
            <TextBlock Margin="0,16,0,0" FontSize="26" Text="{Binding Pump.PumpId, StringFormat=Pompa ID: {0}, TargetNullValue=Canlı veri yok}" />
            <TextBlock Text="{Binding Pump.Status}" />
            <ComboBox Margin="0,10,0,0" Style="{StaticResource DarkComboBoxStyle}" SelectedValue="{Binding Metric}" SelectedValuePath="Tag">
                <ComboBoxItem Content="Debi" Tag="flow_rate" /><ComboBoxItem Content="Basınç" Tag="pressure" />
                <ComboBoxItem Content="Motor akımı" Tag="motor_current" /><ComboBoxItem Content="Sıcaklık" Tag="temperature" />
            </ComboBox>
            <UniformGrid Margin="0,14,0,0" Columns="3">
                <TextBlock Text="{Binding Pump.FlowRate, StringFormat=Debi: {0:N1}}" /><TextBlock Text="{Binding Pump.Pressure, StringFormat=Basınç: {0:N1}}" />
                <TextBlock Text="{Binding Pump.MotorCurrent, StringFormat=Motor akımı: {0:N1}}" /><TextBlock Text="{Binding Pump.Temperature, StringFormat=Sıcaklık: {0:N1}}" />
                <TextBlock Text="{Binding Pump.ErrorCount, StringFormat=Hata sayısı: {0}}" /><TextBlock Text="{Binding Pump.WorkingDuration, StringFormat=Çalışma süresi: {0:N1}}" />
            </UniformGrid>
            <lvc:CartesianChart Height="300" Margin="0,16,0,0" Background="{DynamicResource AppBackgroundBrush}" Series="{Binding Series}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" />
            <controls:AiAnalysisControl Margin="0,18,0,20" Result="{Binding AiAnalysis}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
